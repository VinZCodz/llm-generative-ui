apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-expense-chat-server.fullname" . }}-test-connection"
  labels:
    {{- include "ai-expense-chat-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-expense-chat-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
